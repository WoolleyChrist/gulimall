spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.177.128:3306/guli_wms?useUnicode=true&characterEncoding=utf8&useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource

mybatis-plus:
  # 配置sql的mapper映射文件的扫描路径
  mapper-locations: classpath:/mapper/**/*.xml
  # 配置主键自增
  global-config:
    db-config:
      id-type: auto